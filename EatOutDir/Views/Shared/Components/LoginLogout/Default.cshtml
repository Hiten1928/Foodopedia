@if (User.Identity.IsAuthenticated)
{
    <div>Hello, @User.Identity.Name</div>
    <a asp-controller="Account" asp-action="Logout">LogOut</a>
}
else
{
    <a class="btn btn-block" asp-controller = "Account" asp-action="Logout"> LogOut </a>
}
